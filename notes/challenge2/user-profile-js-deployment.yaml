apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-profile-js-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-profile-js-api
  template:
    metadata:
      labels:
        app: user-profile-js-api
    spec:
      containers:
      - name: user-profile-js-api
        image: registryamd4608.azurecr.io/tripinsights/userprofile:1.0
        env:
          - name: SQL_USER
            value: "<sql user name>"
          - name: SQL_PASSWORD
            value: "<password>"
          - name: SQL_SERVER
            value: "<sql server>.database.windows.net"
          - name: OPENAPI_DOCS_URI
            value: ""
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi
        ports:
        - containerPort: 80
